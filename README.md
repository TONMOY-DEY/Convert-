# PDF Generation Using Python

This project demonstrates how to create a simple PDF file using Python. We use the `reportlab` library for PDF generation.

## Requirements

- Python 3.x
- Install `reportlab` library:

```bash
pip install reportlab
